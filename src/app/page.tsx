import Image from "next/image";

export default function Home() {
    return <main className="flex min-h-screen flex-col items-center justify-between p-24 bg-primary dark:bg-primary-foreground dark:text-primary text-primary-foreground dark:bg-yellow-200">APP</main>;
}
